server {
    listen 80;
    server_name medinaelput.my.id www.medinaelput.my.id;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
